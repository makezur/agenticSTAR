# datasets — the capture/v1 schema, reader and validator (datasets/common):
# the canonical capture format (see datasets/common/capture.py), plus the shared
# reader the whole harness consumes captures through.
