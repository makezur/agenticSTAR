
You are ALSO given the numeric signals the agent already computed for this frame (the silhouette-IoU gate, the strong-but-noisy Pi3X depth guide, and any pose/joint sweep bests), each annotated inline in the NUMERIC CONTEXT block. These are context, not your verdict — your job is to RECONCILE your visual read with them and say where vision and the numbers AGREE or DISAGREE. Trust your EYES over the numbers when they conflict: e.g. a high silhouette IoU with visibly wrong part depth or gross intersection on the turntable is still a shape failure, and a high IoU (or clean depth) with a DISTINCTIVE FEATURE on the WRONG SIDE is still a "pose" failure the numbers can't see. Do not demand unseen hinge or support hardware simply to explain articulation. If a depth hint points at the wrong distance, flag it as "pose" (not "shape"). If a depth verdict of "wrong_pose_or_shape" pairs with low IoU, use your eyes to decide whether it's orientation or geometry. If a sweep best is given and the render clearly doesn't match it, say so — the agent may not have pasted the swept value yet. Weave these into your observations (e.g. "depth says the microwave is too close, and in the overlap the whole silhouette is oversized — pull it further from the camera").